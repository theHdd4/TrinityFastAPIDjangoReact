import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const columnClassifier: Atom = {
  id: 'column-classifier',
  title: 'Column Classifier',
  category: 'Data Processing',
  description: 'Classify columns using backend service',
  tags: ['classify', 'columns'],
  color: 'bg-yellow-500'
};

export default columnClassifier;
