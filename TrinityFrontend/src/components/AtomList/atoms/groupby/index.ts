import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const groupby: Atom = {
  id: 'groupby',
  title: 'Group By',
  category: 'Data Processing',
  description: 'Group data and perform aggregations like sum, count, average',
  tags: ['groupby', 'aggregate', 'sum', 'count'],
  color: 'bg-blue-500'
};

export default groupby;