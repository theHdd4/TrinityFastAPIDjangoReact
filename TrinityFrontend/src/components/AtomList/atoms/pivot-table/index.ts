import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const pivotTable: Atom = {
  id: 'pivot-table',
  title: 'Pivot Table',
  category: 'Business Intelligence',
  description: 'Interactively summarize dimensions and metrics using spreadsheet-style pivot tables.',
  tags: ['pivot', 'aggregation', 'business'],
  color: 'bg-emerald-500'
};

export default pivotTable;

